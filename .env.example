# Copy file này thành .env và điền giá trị thật
# cp .env.example .env

# URL vLLM server trên Kaggle (từ ngrok/cloudflared tunnel — Cell 4 trong notebook)
VLLM_NGROK_URL=https://xxxx.ngrok-free.app

# URL embedding service trên Kaggle (Cell 5 trong notebook)
EMBED_NGROK_URL=https://yyyy.ngrok-free.app

# LangSmith tracing (Integration 10) — lấy key tại smith.langchain.com
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=lab28-platform
